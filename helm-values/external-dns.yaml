provider: aws

env:
  - name: AWS_DEFAULT_REGION
    value: eu-west-2

serviceAccount:
  create: true
  name: external-dns
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::688372068827:role/external-dns"

domainFilters:
  - lab.moamiin.org

policy: sync

registry: txt
txtOwnerId: "external-dns"

rbac:
  create: true
