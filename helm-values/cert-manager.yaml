crds:
  enabled: true

serviceAccount:
  create: true
  name: cert-manager
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::688372068827:role/cert-manager"

extraArgs:
  - --dns01-recursive-nameservers="8.8.8.8:53"
  - --dns01-recursive-nameservers-only

prometheus:
  enabled: false
