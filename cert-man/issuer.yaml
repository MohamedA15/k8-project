apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: issuer
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: "mo684073@gmail.com"
    privateKeySecretRef:
      name: issuer
    solvers:
      - dns01:
          route53:
            region: eu-west-2
            hostedZoneID: Z00529853TL3ZW3XE1AFE
        selector:
          dnsZones:
            - lab.moamiin.org
      - http01:
          ingress:
            class: nginx
